apply plugin: 'application'
apply plugin: 'jacoco'

mainClassName = "waveapp.WaveApp"

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes(
            'Main-Class': 'waveapp.WaveApp'
        )
    }
}
