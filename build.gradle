apply plugin: 'application'
apply plugin: 'jacoco'

mainClassName = "WaveApp"

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes(
            'Main-Class': 'WaveApp'
        )
    }
}
